flask==2.2.2
supabase-py==0.5.1  # Use a valid version; check PyPI for available versions.
torch==1.13.1       # Adjust based on your requirements; ensure it matches your system.
torchvision==0.14.1  # Adjust based on your requirements; ensure it matches your system.
transformers==4.21.1  # Check for latest version if needed.
Pillow==9.3.0         # Check for latest version if needed.
requests==2.28.1      # Check for latest version if needed.
gunicorn==20.1.0      # Optional for production use; can be used for local testing.
